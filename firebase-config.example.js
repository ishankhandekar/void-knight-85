// TEMPLATE — copy this file to `firebase-config.js` and fill in your own Firebase web config
// (Firebase Console → Project settings → "Your apps" → SDK setup and configuration).
//
// `firebase-config.js` is gitignored so no project's real config is committed to this public repo.
// These are public client keys; secure your project with Firestore rules + API-key restrictions
// (Google Cloud Console → Credentials) + Firebase App Check, not by hiding the values.
export const firebaseConfig = {
  apiKey: 'YOUR_API_KEY',
  authDomain: 'YOUR_PROJECT.firebaseapp.com',
  projectId: 'YOUR_PROJECT',
  storageBucket: 'YOUR_PROJECT.firebasestorage.app',
  messagingSenderId: 'YOUR_SENDER_ID',
  appId: 'YOUR_APP_ID',
};
