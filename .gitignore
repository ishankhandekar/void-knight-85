node_modules
package-lock.json
bun.lock
.vscode

# Local Firebase CLI project alias (firebase deploy reads it) — not needed by the deployed game.
.firebaserc

# Local tooling / scratch — never push these
.bughunt-baseline/
.rulestest/
firestore-debug.log
ui-debug.log
firebase-debug.log
