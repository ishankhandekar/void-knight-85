node_modules
package-lock.json
bun.lock
.vscode

# Personal Firebase config — public client keys, but kept out of this shared public repo.
# Copy firebase-config.example.js to firebase-config.js to set up a checkout.
firebase-config.js
.firebaserc

# Local tooling / scratch — never push these
.bughunt-baseline/
.rulestest/
firestore-debug.log
ui-debug.log
firebase-debug.log
